{
  "name": "js-perplexions",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predev": "node scripts/split-levels.js levels.txt",
    "dev": "vite",
    "prebuild": "node scripts/split-levels.js levels.txt",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^6.4.3",
    "vitest": "^2.1.9"
  }
}
